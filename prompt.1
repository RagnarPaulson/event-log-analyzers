I have a log file that contain entries,one per line, each line begins with a date in the format "2026-01-19T18:29:47.818+00:00".   There are CLIENT_CLIENT_CONNECT entries that contain a field "from <ip:port>" to identify the source.  Some time later there is a CLIENT_CLIENT_DISCONNECT entry with a 'conn()' field in the format "conn(0, 0, 172.16.35.180:58362, CLSWT, 0, 0, 0)" where the third entry is the <ip:port> of the session disconnecting.  Write me a python script that will parse this file ignoring lines that contain neither CLIENT_CLIENT_CONNECT nor CLIENT_CLIENT_DISCONNECT, ignoring CLIENT_CLIENT_DISCONNECT that do not have a previous connect based on <ip:port>.  Count the number of unique ip source addresses not including port.  For each ip source address prepare a report  that includes total number of connections from each.  average length of the connections for each.  Maximum number of connections at any one time for each.  And the number of disconnects that include 'Peer TCP Reset' for each
